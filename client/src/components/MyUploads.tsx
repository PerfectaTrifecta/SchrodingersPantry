import React from 'react'

const MyUploads = () => {
//create a useEffect function that makes axios request to backend for that users uploads, should be an array of objects, cloudinary url strings and recipe names.

//sets state of user uploads and map through it, creating a styled component with each one



/*
all cards/components should be responsive and adjustable
**/

  return (
    <div>
      
    </div>
  )
}

export default MyUploads;
